AI prompt composer. The hero of Pulse's AI builder — type a session description, get drafted activities.

```jsx
<AIComposer
  value={prompt}
  onChange={setPrompt}
  onGenerate={run}
  suggestions={['Icebreaker for 50 new hires', '5-question product quiz', 'Sprint retro: start/stop/continue']}
/>
```
