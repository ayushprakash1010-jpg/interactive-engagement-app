Surface container. `interactive` for clickable tiles, `tone="ai"` for AI panels, `tone="dashed"` for empty states.

```jsx
<Card interactive>{/* event tile */}</Card>
<Card tone="ai" padding="md">{/* AI suggestions */}</Card>
<Card tone="dashed">No events yet</Card>
```
