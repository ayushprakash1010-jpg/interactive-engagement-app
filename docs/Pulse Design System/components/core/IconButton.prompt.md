Icon-only button for toolbars / card affordances. Always pass `label`.

```jsx
<IconButton label="Settings"><Settings size={18} /></IconButton>
<IconButton variant="outline" label="Copy code"><Copy size={18} /></IconButton>
```
