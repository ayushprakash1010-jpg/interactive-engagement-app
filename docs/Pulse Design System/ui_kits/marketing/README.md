# Marketing UI kit — Pulse

The public landing page. Recreated from `apps/web/src/app/page.tsx` (hero,
stats, features, how-it-works, CTA), restyled to the Pulse identity and extended
with a **Pulse AI** band — the "describe it, Pulse drafts it" story that anchors
the future-ready positioning. Composes `Button`, `Card`, `Badge`, `AIBadge`,
`JoinCode`. Open `index.html`.
